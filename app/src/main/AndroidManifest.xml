<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Necessária a partir do Android 13 (API 33) pra quem posta a notificação
         (agora o app, via delegação) — sem isso a delegação funciona mas a
         notificação some -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.LaunchDefault">

        <activity
            android:name=".UpdateActivity"
            android:label="@string/app_name"
            android:exported="true">

            <!-- Endereço do seu site (a PWA já hospedada na Vercel) -->
            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://sims4androidtranslator.vercel.app" />

            <!-- Some com a barra de status enquanto carrega, igual um app nativo -->
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/theme_color" />

            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/background_color" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Isso faz o Android abrir links do seu domínio DENTRO do app,
                 em vez de jogar pro navegador -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="sims4androidtranslator.vercel.app" />
            </intent-filter>
        </activity>

        <!-- Delega a exibição das notificações pro app: sem o intent-filter e o
             SMALL_ICON abaixo, o Chrome não descobre esse serviço e acaba
             mostrando as notificações por conta própria (ícone e agrupamento
             do Chrome em vez do app) -->
        <service
            android:name=".BrandedNotificationDelegationService"
            android:enabled="true"
            android:exported="true">

            <meta-data
                android:name="android.support.customtabs.trusted.SMALL_ICON"
                android:resource="@drawable/ic_stat_notify" />

            <intent-filter>
                <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
            android:resource="@drawable/splash" />
        <meta-data
            android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
            android:resource="@color/background_color" />
        <meta-data
            android:name="android.support.customtabs.trusted.SPLASH_SCREEN_FADE_OUT_DURATION"
            android:value="300" />

    </application>
</manifest>
